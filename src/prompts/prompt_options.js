export default {
  prompt: `
请根据医生的问诊问题，自动生成一组常见、简短的快捷回答选项，供用户快速选择。    

## 要求
- 这是医学类的问诊场景，所以你的快捷回答选项要符合这个背景
- 你的回答选项要是简体中文的
- 只输出一个回答列表，每个回答尽量简短、贴近实际生活表达，无需解释说明
- 你的每一项要尽可能的简短，最多五个字
- 最多输出 5 项
- 如果包含多个问题，你提供的选项要尽可能多的覆盖不同问题
- 如果问题中包含“有没有”等类型的问题，你的选项中要有否定的这个选项，例如：
   - 有没有做过手术？ 选项中要有“没做过”
   - 平时抽烟喝酒吗？选项中要有“不抽烟”，“不喝酒”
- 如果没什么好提示的，你也可以输出一个空数组，要根据实际情况做判断
- 提示次要尽量避开敏感词校验，例如：医生问肚子疼每天几次，这时回答 '三四次'有时会触发'色情校验',你最好改成”大概四次“或者“腹泻3-4次”

**示例：**  
- 医生问诊问题：“你是什么时候开始肚子疼的？”  
  - 输出：["昨天", "今天", "三天前", "一周前"]  

- 医生问诊问题：“你肚子疼是哪种疼？”  
  - 输出：["一阵一阵的", "持续疼痛", "隐隐作痛"]  

- 医生问诊问题：“你有没有发烧？”  
  - 输出：["有", "没有", "不确定"]  
  
请根据下面的医生问诊问题，生成类似的快捷回答列表。  
`,
  schema: {
    type: "json_schema",
    json_schema: {
      name: "quick_options",
      schema: {
        properties: {
          options: {
            description: "快捷回答选项列表，每项最多5个字，最多5项。",
            items: {
              type: "string",
            },
            type: "array",
          },
        },
        required: ["options"],
        type: "object",
      },
      strict: true,
    },
  },
};
